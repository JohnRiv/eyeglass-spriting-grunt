# eyeglass-spriting-grunt
Example project using eyeglass, eyeglass-spriting, and grunt-sass

## OSX Error when running `$ grunt`:
```bash
Running "sass:dist" (sass) task
Fatal error: Path must be a string. Received undefined
```

## Windows 10 Error when running `$ grunt`:
```bash
Running "sass:dist" (sass) task
>> no mixin named media
>>
>> Backtrace:
>>      assets/sass/styles.scss:34
>>   Line 34  Column 10  assets\sass\styles.scss
```